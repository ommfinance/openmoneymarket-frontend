export enum IconexId {
  DEFAULT = 1,
  SHOW_MESSAGE_HIDE_MODAL = 2, // this indicates that backlogged message should be shown when transaction is transmitted
  SHOW_MESSAGE_KEEP_MODAL = 3 // show both message and modal
}
