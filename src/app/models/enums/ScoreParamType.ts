export enum ScoreParamType {
  INT = "int",
  ADDRESS = "Address",
  BYTES = "bytes",
  BOOLEAN = "bool",
  STRUCT = "struct",
}

export enum ScorePayloadParamType {
  BIG_INTEGER = "BigInteger",
  ADDRESS = "Address",
  BYTES = "Bytes"
}
